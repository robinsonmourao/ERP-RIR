default:  -p ci  
          -p test
          -p fast_mode
          -p terminal_drop_lines_each_case
          -p headless
          --publish-quiet

pretty: --format pretty
ci: --format progress
test: ENVIRONMENT_TYPE=test
headless: HEADLESS=headless
fast_mode: FAST_MODE=false
terminal_drop_lines_each_case: TERMINAL_DROP_LINES_EACH_CASE=true