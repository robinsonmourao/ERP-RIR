<%= render 'builders/render_templates/object', object: contato, 
                                               header_titles: ['Nome da pessoa', 
                                                               '001[Código do contato]-002[Código do tipo do contato]'],
                                               cells: [contato.nome_pessoa, contato.codigo_contato_composto] %>
